/**
 * DEPRECIADO — Usar @/lib/meta-whatsapp diretamente.
 * Este arquivo existe apenas como shim de compatibilidade.
 * A integração Z-API foi removida. Usamos a API oficial da Meta.
 */
export { sendWhatsApp, sendWhatsAppTemplate } from "@/lib/meta-whatsapp";
