{
  "name": "serigraphecia-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    ".",
    "admin"
  ],
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "admin:dev": "npm run dev --workspace=admin",
    "admin:build": "npm run build --workspace=admin",
    "admin:start": "npm run start --workspace=admin"
  },
  "dependencies": {
    "astro": "^5.7.0",
    "@vercel/analytics": "^1.0.0"
  }
}
